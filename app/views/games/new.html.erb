<div class="container mt-5">
  <div class="card">
    <div class="card-body">
      <h1 class="card-title mb-4"><%= 'New Game' %></h1>
      <%= render "form", game: @game %>

      <div class="mt-3">
        <%= link_to "Back", games_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
